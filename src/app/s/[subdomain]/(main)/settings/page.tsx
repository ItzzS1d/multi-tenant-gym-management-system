import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "Settings",
    description: "Configure your gym's settings and preferences.",
};

const Settings = () => {
    return <div></div>;
};

export default Settings;
