import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "Dashboard",
    description: "View your gym's key performance indicators and recent activity.",
};

const Dashboard = () => {
    return <div></div>;
};

export default Dashboard;
